const api = process.env.NEXT_PUBLIC_API_BASE_URL ?? "/api/v1";
export type Result = { image_id: string; original_filename: string; image_status: string; ai_status: string; customer_id_ai: string | null; meter_reading_ai: string | null; final_confidence: number; review_status: string | null; final_customer_id: string | null; final_meter_reading: string | null };
export async function getResults(): Promise<Result[]> { const r = await fetch(`${api}/results`, { credentials: "include" }); if (!r.ok) throw new Error("Unable to load results."); return r.json(); }
export async function reviewResult(id: string, csrf: string, action: "CONFIRM" | "REJECT", customer: string, reading: string): Promise<void> { const r = await fetch(`${api}/results/${id}/review`, { method: "PUT", credentials: "include", headers: { "Content-Type": "application/json", "X-CSRF-Token": csrf }, body: JSON.stringify({ action, final_customer_id: customer || null, final_meter_reading: reading || null }) }); if (!r.ok) { const b: { detail?: string } = await r.json().catch(() => ({})); throw new Error(b.detail ?? "Review failed."); } }
