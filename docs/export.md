# Excel export

Only confirmed final readings are exported. The workbook includes final values, never replaces AI output, freezes the header, filters rows, and escapes leading formula characters (`=`, `+`, `-`, `@`) to prevent Excel formula injection. Generated files remain in authenticated local storage and are audited.
